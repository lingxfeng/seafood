/**使用Disco平台开发前端界面的通用JS都统一放在此文件中，如分页跳转JS函数**/

/**
 * 导向页面函数
 * @param {} n 当前页
 * @param {} dirId 当前栏目ID
 */
function gotoPage(n, dirId) {
    //location.href = "/news.java?cmd=moreDocList&currentPage=" + n + "&id=$!{dir.id}";
}